name: pushape_flutter
description: flutter pushape notification package
version: 0.0.1
author: GlueLabs / Giuse Petroso
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.1
  device_info: ^0.4.2+4
  firebase_messaging: ^6.0.16

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: